{% extends 'baseBacK.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
<div class="container-xxl flex-grow-1 container-p-y">

    <div style=" display: flex;align-content: center ; justify-content: center ; margin-top: 100px ; padding: 20px">
        <div class="col-md-6">
            <div class="card mb-4">
    <h1 style="text-align: center" >Evenement</h1>

    <table class="table" border="10px">
        <tbody>
            <tr>
                <th style="text-align: center">Id de l'evenement</th>
                <td>{{ evenement.idevent }}</td>
            </tr>
            <tr>
                <th style="text-align: center" > Nom de l'evenement</th>
                <td>{{ evenement.nomevent }}</td>
            </tr>
            <tr>
                <th style="text-align: center" >Prix de l'evenement</th>
                <td>{{ evenement.prixevent }}</td>
            </tr>
            <tr>
                <th style="text-align: center" >Date de l'éevenement</th>
                <td>{{ evenement.date ? evenement.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

                <div class="row mb-3" style="margin-left: 250px">
                    <div class="col-sm-10">
    <a type="submit" class="btn btn-primary"href="{{ path('app_evenement_back_index') }}">back to list</a>
    <a type="submit" class="btn btn-primary"href="{{ path('app_evenement_back_edit', {'idevent': evenement.idevent}) }}">edit</a>
    {{ include('evenement_back/_delete_form.html.twig') }}
    </div>
</div>
    </div>
{% endblock %}