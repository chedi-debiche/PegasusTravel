<form method="post" action="{{ path('app_reservationevenement_delete', {'idre': reservationevenement.idre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationevenement.idre) }}">
    <button class="btn">Delete</button>
</form>
